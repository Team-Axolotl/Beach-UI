import { matches, closest, getMarginBox } from './dom';
import { getDimensions } from './positioning';
import { capitalizeFirstLetter, lowerCaseFirstLetter } from './utils';

export default {
    matches,
    closest,
    getMarginBox,
    getDimensions,
    capitalizeFirstLetter,
    lowerCaseFirstLetter
};